Classroom Info
<%= render partial: 'classroom', locals: { classroom: @classroom} %>

Students in class:

  <%=  @classroom.students.each do |student| %>
    <li>
      <%= render partial: 'students/student', locals: { student: student} %><br />
    </li>
  <% end %>